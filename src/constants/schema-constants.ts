export const emailConstants = {
  minLength: 1,
  maxLength: 64,
};

export const passwordConstants = {
  minLength: 8,
  maxLength: 64,
};

export const nameConstants = {
  minLength: 2,
  maxLength: 64,
};
